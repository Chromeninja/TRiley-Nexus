---
profiles: "{}"
---

Company profile overrides for timeline and narrative views.

Use frontmatter key `profiles` to define organization metadata.
Each key should match the project `organization` value exactly.
