<div class="atlas-legend" aria-label="Timeline legend">
  <span class="atlas-legend__item"><i class="atlas-legend__swatch atlas-legend__swatch--company"></i>Company Span</span>
  <span class="atlas-legend__item"><i class="atlas-legend__swatch atlas-legend__swatch--project"></i>Project</span>
  <span class="atlas-legend__item"><i class="atlas-legend__swatch atlas-legend__swatch--live"></i>Live / Present</span>
</div>

<style>
  .atlas-legend {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
    align-items: center;
    color: var(--color-text-muted);
    font-size: 0.74rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    font-family: var(--font-mono);
  }

  .atlas-legend__item {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
  }

  .atlas-legend__swatch {
    width: 1.1rem;
    height: 0.45rem;
    border-radius: 999px;
    border: 1px solid var(--color-border);
    display: inline-block;
  }

  .atlas-legend__swatch--company {
    background: linear-gradient(90deg, color-mix(in srgb, var(--color-accent), black 36%), color-mix(in srgb, var(--color-accent), white 10%));
  }

  .atlas-legend__swatch--project {
    background: linear-gradient(90deg, color-mix(in srgb, #5cc8ff, black 24%), color-mix(in srgb, #5cc8ff, white 8%));
  }

  .atlas-legend__swatch--live {
    background: linear-gradient(90deg, color-mix(in srgb, var(--color-success), black 35%), color-mix(in srgb, var(--color-success), white 8%));
    box-shadow: 0 0 14px rgba(74, 222, 128, 0.35);
  }
</style>
