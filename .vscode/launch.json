{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Astro: Start Local Dev Server",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --host localhost --port 4321",
      "cwd": "${workspaceFolder}"
    }
  ]
}
